let a = 10 // determina variavel (a) com valor 10 

if(a > 0){                      // se a variavel (a) for maior que (0)
    console.log('positive')     // imprima 'positive'
    }else if(a < 0){            // se a variavel (a) for manor que (0)
        console.log('negative') // imprima 'negative'
        }else{                  // se nao
            console.log('zero') // imprima 0
        }